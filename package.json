{
  "name": "shoppay",
  "version": "1.0.0",
  "description": "ShopPay full-stack ecommerce: React client + Express/MongoDB server",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run start:dev --prefix server\" \"npm start --prefix client\"",
    "start:server": "npm run start:prod --prefix server",
    "build:client": "npm run build --prefix client",
    "start:client": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
